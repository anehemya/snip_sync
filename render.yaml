services:
  - type: web
    name: snip-sync
    env: node
    buildCommand: |
      npm install
      cd server && npm install
      cd ../clients && npm install && npm run build
    startCommand: cd server && node server.js
    envVars:
      - key: NODE_ENV
        value: production 